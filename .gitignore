# Env
.env
.env.local

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Node
node_modules/
.next/
out/
npm-debug.log*

# Data / state
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
checkpoints.db

# OS
.DS_Store

# Editor
.vscode/
.idea/
.gstack/

# Local tooling state (not project source)
.claude/

# Throwaway seeded GitHub demo repo clone — a separate repo the agent opens
# real PRs against, not part of this project's source (has its own .git,
# must never be tracked as a submodule/gitlink here)
/data-firefighter-demo/
